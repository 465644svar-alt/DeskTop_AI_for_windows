# НАЗНАЧЕНИЕ ФАЙЛА: Сервис журналирования: логи запросов/ошибок и вычисление метрик.
"""
Application Logger with metrics tracking
"""

import os  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
import json  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
import logging  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
from datetime import datetime  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
from typing import Dict, List, Optional  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
from collections import deque  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
from dataclasses import dataclass, asdict  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
from statistics import mean, median  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.


@dataclass  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
# ЛОГИЧЕСКИЙ БЛОК: класс `ResponseLogEntry` — объединяет состояние и поведение подсистемы.
class ResponseLogEntry:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    """Log entry for API response"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    timestamp: str  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    provider: str  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    question: str  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    response: str  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    elapsed_time: float  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    success: bool  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    tokens_used: int = 0  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    model: str = ""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.


@dataclass  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
# ЛОГИЧЕСКИЙ БЛОК: класс `ErrorLogEntry` — объединяет состояние и поведение подсистемы.
class ErrorLogEntry:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    """Log entry for errors"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    timestamp: str  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    provider: str  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    error: str  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    details: str  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    error_code: int = 0  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    retryable: bool = False  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.


@dataclass  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
# ЛОГИЧЕСКИЙ БЛОК: класс `ProviderMetrics` — объединяет состояние и поведение подсистемы.
class ProviderMetrics:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    """Metrics for a single provider"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    total_requests: int = 0  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    successful_requests: int = 0  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    failed_requests: int = 0  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    total_time: float = 0.0  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    total_tokens: int = 0  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    @property  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    # ЛОГИЧЕСКИЙ БЛОК: функция `success_rate` — выполняет отдельный шаг бизнес-логики.
    def success_rate(self) -> float:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Учебный комментарий: функция `success_rate`. Добавьте доменную детализацию при необходимости."""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
        if self.total_requests == 0:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            return 0.0  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        return self.successful_requests / self.total_requests * 100  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    @property  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    # ЛОГИЧЕСКИЙ БЛОК: функция `avg_response_time` — выполняет отдельный шаг бизнес-логики.
    def avg_response_time(self) -> float:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Учебный комментарий: функция `avg_response_time`. Добавьте доменную детализацию при необходимости."""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
        if self.successful_requests == 0:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            return 0.0  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        return self.total_time / self.successful_requests  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `to_dict` — выполняет отдельный шаг бизнес-логики.
    def to_dict(self) -> dict:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Учебный комментарий: функция `to_dict`. Добавьте доменную детализацию при необходимости."""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        return {  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            "total_requests": self.total_requests,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            "successful_requests": self.successful_requests,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            "failed_requests": self.failed_requests,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            "success_rate": round(self.success_rate, 1),  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            "avg_response_time": round(self.avg_response_time, 2),  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            "total_tokens": self.total_tokens  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        }  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.


# ЛОГИЧЕСКИЙ БЛОК: класс `AppLogger` — объединяет состояние и поведение подсистемы.
class AppLogger:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    """Application logger with response/error tracking and metrics"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `__init__` — выполняет отдельный шаг бизнес-логики.
    def __init__(self, log_dir: str = "logs", max_responses: int = 1000, max_errors: int = 500):  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Учебный комментарий: функция `__init__`. Добавьте доменную детализацию при необходимости."""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self.log_dir = log_dir  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self.session_start = datetime.now()  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        # In-memory logs with size limits
        self.responses_log: deque = deque(maxlen=max_responses)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self.errors_log: deque = deque(maxlen=max_errors)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        # Provider metrics
        self.metrics: Dict[str, ProviderMetrics] = {}  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        # Recent response times for each provider (for trend analysis)
        self._response_times: Dict[str, deque] = {}  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        # Create log directory
        os.makedirs(log_dir, exist_ok=True)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        # Setup file logging
        self._setup_file_logging()  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `_setup_file_logging` — выполняет отдельный шаг бизнес-логики.
    def _setup_file_logging(self):  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Setup file-based logging"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        log_file = os.path.join(  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            self.log_dir,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            f"app_{self.session_start.strftime('%Y%m%d_%H%M%S')}.log"  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        )  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        # Create a new handler for this session
        file_handler = logging.FileHandler(log_file, encoding='utf-8')  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        file_handler.setLevel(logging.INFO)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        file_handler.setFormatter(  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            logging.Formatter('%(asctime)s | %(levelname)s | %(name)s | %(message)s')  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        )  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        # Get root logger and add handler
        root_logger = logging.getLogger()  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        root_logger.setLevel(logging.INFO)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        root_logger.addHandler(file_handler)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        # Also log to console in debug mode
        # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
        if not any(isinstance(h, logging.StreamHandler) for h in root_logger.handlers):  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            console_handler = logging.StreamHandler()  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            console_handler.setLevel(logging.WARNING)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            root_logger.addHandler(console_handler)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        self.logger = logging.getLogger(__name__)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self.logger.info(f"Session started at {self.session_start}")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `_ensure_metrics` — выполняет отдельный шаг бизнес-логики.
    def _ensure_metrics(self, provider: str):  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Ensure metrics exist for provider"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
        if provider not in self.metrics:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            self.metrics[provider] = ProviderMetrics()  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
        if provider not in self._response_times:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            self._response_times[provider] = deque(maxlen=100)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `log_response` — выполняет отдельный шаг бизнес-логики.
    def log_response(  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        provider: str,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        question: str,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        response: str,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        elapsed: float,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        success: bool = True,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        tokens_used: int = 0,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        model: str = ""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    ):  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Log AI response and update metrics"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self._ensure_metrics(provider)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        entry = ResponseLogEntry(  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            timestamp=datetime.now().isoformat(),  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            provider=provider,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            question=question[:500],  # Truncate for log  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            response=response[:5000] if success else response,  # Limit response size  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            elapsed_time=elapsed,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            success=success,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            tokens_used=tokens_used,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            model=model  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        )  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self.responses_log.append(asdict(entry))  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        # Update metrics
        metrics = self.metrics[provider]  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        metrics.total_requests += 1  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
        if success:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            metrics.successful_requests += 1  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            metrics.total_time += elapsed  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            metrics.total_tokens += tokens_used  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            self._response_times[provider].append(elapsed)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
        else:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            metrics.failed_requests += 1  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        # Log to file
        status = "SUCCESS" if success else "FAILED"  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self.logger.info(f"[{provider}] {status} | {elapsed:.2f}s | Q: {question[:100]}...")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `log_error` — выполняет отдельный шаг бизнес-логики.
    def log_error(  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        provider: str,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        error: str,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        details: str = "",  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        error_code: int = 0,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        retryable: bool = False  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    ):  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Log error with details"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        entry = ErrorLogEntry(  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            timestamp=datetime.now().isoformat(),  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            provider=provider,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            error=error,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            details=details[:1000],  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            error_code=error_code,  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            retryable=retryable  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        )  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self.errors_log.append(asdict(entry))  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

        self.logger.error(f"[{provider}] {error} | Code: {error_code} | {details[:200]}")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `get_responses_log` — выполняет отдельный шаг бизнес-логики.
    def get_responses_log(self) -> List[dict]:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Get responses log"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        return list(self.responses_log)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `get_errors_log` — выполняет отдельный шаг бизнес-логики.
    def get_errors_log(self) -> List[dict]:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Get errors log"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        return list(self.errors_log)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `get_provider_metrics` — выполняет отдельный шаг бизнес-логики.
    def get_provider_metrics(self, provider: str) -> Optional[dict]:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Get metrics for a specific provider"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
        if provider in self.metrics:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            return self.metrics[provider].to_dict()  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        return None  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `get_all_metrics` — выполняет отдельный шаг бизнес-логики.
    def get_all_metrics(self) -> Dict[str, dict]:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Get metrics for all providers"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        return {name: m.to_dict() for name, m in self.metrics.items()}  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `get_response_time_trend` — выполняет отдельный шаг бизнес-логики.
    def get_response_time_trend(self, provider: str) -> List[float]:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Get recent response times for trend analysis"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
        if provider in self._response_times:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            return list(self._response_times[provider])  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        return []  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `export_logs` — выполняет отдельный шаг бизнес-логики.
    def export_logs(self, filepath: str, log_type: str = "all") -> bool:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Export logs to file"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        # ЛОГИЧЕСКИЙ БЛОК: обработка ошибок и устойчивость выполнения.
        try:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            with open(filepath, 'w', encoding='utf-8') as f:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                f.write("=" * 70 + "\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                f.write(f"AI Manager Log Export\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                f.write(f"Session: {self.session_start.strftime('%Y-%m-%d %H:%M:%S')}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                f.write(f"Export: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                f.write("=" * 70 + "\n\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

                # Metrics summary
                f.write("PROVIDER METRICS\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                f.write("-" * 50 + "\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                # ЛОГИЧЕСКИЙ БЛОК: цикл для поэтапной обработки данных.
                for name, metrics in self.metrics.items():  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    m = metrics.to_dict()  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    f.write(f"\n{name}:\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    f.write(f"  Requests: {m['total_requests']} (Success: {m['success_rate']:.1f}%)\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    f.write(f"  Avg Time: {m['avg_response_time']:.2f}s\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    f.write(f"  Tokens: {m['total_tokens']}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                f.write("\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

                # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
                if log_type in ["all", "responses"]:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    f.write("=" * 70 + "\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    f.write("RESPONSES LOG\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    f.write("=" * 70 + "\n\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    # ЛОГИЧЕСКИЙ БЛОК: цикл для поэтапной обработки данных.
                    for entry in self.responses_log:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        f.write(f"[{entry['timestamp'][:19]}] {entry['provider']}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        f.write(f"Model: {entry.get('model', 'N/A')}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        f.write(f"Q: {entry['question']}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        status = "OK" if entry['success'] else "FAIL"  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        f.write(f"Status: {status} | Time: {entry['elapsed_time']:.2f}s\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        f.write(f"Response: {entry['response'][:500]}...\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        f.write("-" * 50 + "\n\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

                # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
                if log_type in ["all", "errors"]:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    f.write("\n" + "=" * 70 + "\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    f.write("ERRORS LOG\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    f.write("=" * 70 + "\n\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                    # ЛОГИЧЕСКИЙ БЛОК: цикл для поэтапной обработки данных.
                    for entry in self.errors_log:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        f.write(f"[{entry['timestamp'][:19]}] {entry['provider']}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        f.write(f"Error: {entry['error']}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        f.write(f"Code: {entry.get('error_code', 'N/A')}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
                        if entry['details']:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                            f.write(f"Details: {entry['details']}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                        f.write("-" * 50 + "\n\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

                f.write("\n" + "=" * 70 + "\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                f.write(f"Total responses: {len(self.responses_log)}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                f.write(f"Total errors: {len(self.errors_log)}\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
                f.write("=" * 70 + "\n")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

            return True  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        # ЛОГИЧЕСКИЙ БЛОК: обработка ошибок и устойчивость выполнения.
        except Exception as e:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            self.logger.error(f"Failed to export logs: {e}")  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
            return False  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `clear_logs` — выполняет отдельный шаг бизнес-логики.
    def clear_logs(self):  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Clear in-memory logs"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self.responses_log.clear()  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self.errors_log.clear()  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.

    # ЛОГИЧЕСКИЙ БЛОК: функция `reset_metrics` — выполняет отдельный шаг бизнес-логики.
    def reset_metrics(self):  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        """Reset all metrics"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self.metrics.clear()  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        self._response_times.clear()  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.


# Singleton instance
_logger_instance: Optional[AppLogger] = None  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.


# ЛОГИЧЕСКИЙ БЛОК: функция `get_logger` — выполняет отдельный шаг бизнес-логики.
def get_logger(log_dir: str = "logs") -> AppLogger:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    """Get or create logger instance"""  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    global _logger_instance  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    # ЛОГИЧЕСКИЙ БЛОК: ветвление условий для выбора дальнейшего сценария.
    if _logger_instance is None:  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
        _logger_instance = AppLogger(log_dir)  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
    return _logger_instance  # ПОЯСНЕНИЕ: строка участвует в реализации текущего шага логики.
